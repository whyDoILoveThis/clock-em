import { Company } from "@/types/types.type";
import React from "react";

interface Props {
  companies: Company[];
}

const MyEmployers = ({ companies }: Props) => {
  return <div></div>;
};

export default MyEmployers;
